---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2025 The Linux Foundation

# Runs on a new pull request, performs build and runs tests
name: 'Python Build/Test'

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
    inputs:
      clear_cache:
        description: 'Clear all Python dependency caches'
        type: boolean
        default: false
        required: false
  pull_request:
    types: [opened, reopened, edited, synchronize]
    branches:
      - main
      - master
    paths:
      - '**'
      - '!.github/**'
      - '!.*'
      - '!tox.ini'

concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

permissions:
  actions: write  # Required for cache deletion when clear_cache is true

jobs:
  repository-metadata:
    name: "Repository Metadata"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    timeout-minutes: 5
    steps:
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: audit

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1
        with:
          fetch-depth: 0

      - name: "Gather repository metadata"
        id: repo-metadata
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/repository-metadata-action@ceabcd987d13d7bfefd2372e01eebb0ddac45956  # v0.2.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          github_summary: 'false'
          gerrit_summary: 'false'
          artifact_upload: 'true'
          artifact_formats: 'json'

  python-metadata:
    name: 'Extract Project Metadata'
    runs-on: ubuntu-latest
    outputs:
      matrix_json: "${{ steps.metadata.outputs.python_matrix_json }}"
      project_name: "${{ steps.metadata.outputs.project_name }}"
    permissions:
      contents: read
    timeout-minutes: 5
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: 'Extract Python project metadata'
        id: metadata
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/build-metadata-action@c8d6f8396301edd325ac90db463850048f5fe20c  # v0.1.1
        with:
          path_prefix: '.'
          output_format: summary
          include_environment: true
          use_version_extract: true
          verbose: true

  python-build-x64:
    name: "Python Build x64 ${{ matrix.python-version }}"
    runs-on: ubuntu-latest
    needs: python-metadata
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.python-metadata.outputs.matrix_json) }}"
    permissions:
      contents: read
      actions: write  # Required for cache deletion when clear_cache is true
    timeout-minutes: 20
    env:
      GH_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: "Build Python project ${{ matrix.python-version }}"
        id: python-build
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-build-action@8f567398591014dd5627961aaa9ec17816b072f0  # v1.0.2
        with:
          artefact_name: python-nss-ng-x64-py${{ matrix.python-version }}
          python_version: ${{ matrix.python-version }}
          auditwheel: true
          manylinux_version: manylinux_2_38
          clear_cache: ${{ github.event.inputs.clear_cache || 'false' }}
          make: true
          make_args: 'deps-nss env-github-actions'

  python-build-arm64:
    name: "Python Build ARM64 ${{ matrix.python-version }}"
    runs-on: ubuntu-24.04-arm
    needs: python-metadata
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.python-metadata.outputs.matrix_json) }}"
    permissions:
      contents: read
      actions: write  # Required for cache deletion when clear_cache is true
    timeout-minutes: 20
    env:
      GH_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: "Build Python project ${{ matrix.python-version }}"
        id: python-build
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-build-action@8f567398591014dd5627961aaa9ec17816b072f0  # v1.0.2
        with:
          artefact_name: python-nss-ng-arm64-py${{ matrix.python-version }}
          python_version: ${{ matrix.python-version }}
          auditwheel: true
          manylinux_version: manylinux_2_38
          clear_cache: ${{ github.event.inputs.clear_cache || 'false' }}
          make: true
          make_args: 'deps-nss env-github-actions'

  python-tests-x64:
    name: "Python Tests x64 ${{ matrix.python-version }}"
    runs-on: 'ubuntu-latest'
    needs: ['python-metadata', 'python-build-x64']
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.python-metadata.outputs.matrix_json) }}"
    permissions:
      contents: read
    timeout-minutes: 12
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: audit

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: Build NSS/NSPR and install dependencies
        run: |
            make deps-nss deps-test env-github-actions
            # Ensure NSS libraries are in ldconfig cache
            sudo ldconfig
            # Export library path for subsequent steps
            LIB_PATH="/usr/lib:/usr/lib/aarch64-linux-gnu"
            LIB_PATH="${LIB_PATH}:/usr/lib/x86_64-linux-gnu"
            echo "LD_LIBRARY_PATH=${LIB_PATH}:${LD_LIBRARY_PATH}" \
              >> "$GITHUB_ENV"

      - name: Verify NSS libraries and environment
        run: |
            echo "=== Library Path Diagnostics ==="
            echo "LD_LIBRARY_PATH=${LD_LIBRARY_PATH}"
            echo "PKG_CONFIG_PATH=${PKG_CONFIG_PATH}"
            echo ""
            echo "=== NSS Libraries in /usr/lib ==="
            ls -la /usr/lib/libnss* /usr/lib/libnspr* 2>/dev/null || \
              echo "Not found in /usr/lib"
            echo ""
            echo "=== NSS Libraries in /usr/lib/x86_64-linux-gnu ==="
            ls -la /usr/lib/x86_64-linux-gnu/libnss* \
              /usr/lib/x86_64-linux-gnu/libnspr* 2>/dev/null || \
              echo "Not found in /usr/lib/x86_64-linux-gnu"
            echo ""
            echo "=== ldconfig cache ==="
            ldconfig -p | grep -E '(nss|nspr)' || \
              echo "NSS/NSPR not in ldconfig cache"
            echo ""
            echo "=== pkg-config ==="
            pkg-config --exists nss && \
              echo "NSS found: $(pkg-config --modversion nss)" || \
              echo "NSS not found by pkg-config"
            pkg-config --exists nspr && \
              echo "NSPR found: $(pkg-config --modversion nspr)" || \
              echo "NSPR not found by pkg-config"

      - name: Verify Python can import nss module
        run: |
            echo "=== Python Import Diagnostics ==="
            python3 -c "import sys; print('Python sys.path:', sys.path)" \
              || true
            echo ""
            echo "=== Attempting to import nss ==="
            python3 -c "import nss; print('SUCCESS: nss imported')" 2>&1 \
              || echo "FAILED: Cannot import nss"
            echo ""
            echo "=== Checking for ImportError details ==="
            python3 -c "
            import sys
            try:
                import nss.nss
                print('SUCCESS: nss.nss imported')
            except ImportError as e:
                print(f'ImportError: {e}')
                import traceback
                traceback.print_exc()
            " 2>&1 || true
            echo ""
            echo "=== Checking installed nss package ==="
            python3 -c "import sys; import site; \
              print('site-packages:', site.getsitepackages())" || true
            PYVER="${{ matrix.python-version }}"
            ls -la /home/runner/.local/lib/python${PYVER}/site-packages/nss/ \
              2>/dev/null || echo "nss package not found"

      - name: Test import with actual Python that will run tests
        run: |
            echo "=== Testing import with hostedtoolcache Python ==="
            PYTHON_PATH="/opt/hostedtoolcache/Python/3.12.12/x64/bin/python3"
            if [ -f "$PYTHON_PATH" ]; then
                echo "Using: $PYTHON_PATH"
                $PYTHON_PATH -c "import sys; print('Python:', sys.executable)"
                echo "Attempting import..."
                $PYTHON_PATH -c "import nss; print('SUCCESS: nss imported')" \
                  || \
                  echo "FAILED to import nss"
                echo ""
                echo "Checking sys.path:"
                $PYTHON_PATH -c "import sys; print('\n'.join(sys.path))"
                echo ""
                echo "Trying to import nss.error:"
                $PYTHON_PATH -c "import nss.error; print('SUCCESS')" \
                  2>&1 || true
            else
                echo "Python not found at $PYTHON_PATH, trying python3.12"
                which python3.12 && \
                  python3.12 -c "import nss; print('SUCCESS')" || \
                  echo "FAILED with python3.12"
            fi

      - name: "Python tests [pytest] ${{ matrix.python-version }}"
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-test-action@92d4110d44ebc18fa4575c6b00203ff67d01a1cb  # v1.0.1
        with:
          python_version: ${{ matrix.python-version }}
          # yamllint disable-line rule:line-length
          artefact_name: python-nss-ng-x64-py${{ matrix.python-version }}-coverage
          report_artefact: 'true'
        env:
          LD_LIBRARY_PATH: /usr/lib:/usr/lib/x86_64-linux-gnu
          # yamllint disable-line rule:line-length
          PYTHONPATH: /opt/hostedtoolcache/Python/3.12.12/x64/lib/python3.12/site-packages

      - name: Post-test diagnostics (if test failed)
        if: failure()
        run: |
            echo "=== POST-TEST DIAGNOSTICS ==="
            echo "Checking where package was actually installed..."
            python3 -m pip list | grep python-nss-ng || \
              echo "Package not in pip list"
            echo ""
            echo "Getting pip show output:"
            python3 -m pip show python-nss-ng || echo "pip show failed"
            echo ""
            echo "Extracting actual installation location:"
            INSTALL_LOC=$(python3 -m pip show python-nss-ng | \
              grep "Location:" | cut -d' ' -f2)
            echo "Package installed at: ${INSTALL_LOC}"
            if [ -n "${INSTALL_LOC}" ]; then
                echo "Checking for nss package in actual location:"
                ls -la "${INSTALL_LOC}/nss/" 2>/dev/null || \
                  echo "nss directory not found at ${INSTALL_LOC}"
                echo ""
                echo "Checking for .so files in actual location:"
                find "${INSTALL_LOC}/nss/" -name "*.so" 2>/dev/null || \
                  echo "No .so files found"
                echo ""
                echo "Checking library dependencies with ldd:"
                for so_file in "${INSTALL_LOC}/nss/"*.so; do
                    if [ -f "$so_file" ]; then
                        echo "--- $(basename "$so_file") ---"
                        ldd "$so_file" 2>&1 | \
                          grep -E "(nss|nspr|not found)" || true
                    fi
                done
            fi
            echo ""
            echo "Searching for nss package in all locations:"
            PYVER="${{ matrix.python-version }}"
            find /home/runner/.local/lib/python${PYVER}/ -name "nss" \
              -type d 2>/dev/null || echo "Not found in .local"
            find /usr/local/lib/python${PYVER}/ -name "nss" \
              -type d 2>/dev/null || echo "Not found in /usr/local"
            find /usr/lib/python3/dist-packages/ -name "nss" \
              -type d 2>/dev/null || echo "Not found in dist-packages"
            echo ""
            echo "Searching for ANY python-nss-ng files:"
            find /home/runner/.local/lib/python${PYVER}/ \
              -name "*python*nss*" 2>/dev/null | head -20 || true
            find /usr/local/lib/python${PYVER}/ \
              -name "*python*nss*" 2>/dev/null | head -20 || true
            echo ""
            echo "Searching for .so files with nss:"
            find /home/runner/.local/lib/python${PYVER}/ \
              -name "*nss*.so" 2>/dev/null || echo "No .so files found"
            echo ""
            echo "Checking pip cache:"
            ls -la /home/runner/.cache/pip/wheels/ 2>/dev/null || true

  python-tests-arm64:
    name: "Python Tests ARM64 ${{ matrix.python-version }}"
    runs-on: 'ubuntu-24.04-arm'
    needs: ['python-metadata', 'python-build-arm64']
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.python-metadata.outputs.matrix_json) }}"
    permissions:
      contents: read
    timeout-minutes: 12
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: audit

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: Build NSS/NSPR and install dependencies
        run: |
          make deps-nss deps-test env-github-actions
          # Ensure NSS libraries are in ldconfig cache
          sudo ldconfig
          # Export library path for subsequent steps
          LIB_PATH="/usr/lib:/usr/lib/aarch64-linux-gnu"
          LIB_PATH="${LIB_PATH}:/usr/lib/x86_64-linux-gnu"
          echo "LD_LIBRARY_PATH=${LIB_PATH}:${LD_LIBRARY_PATH}" >> "$GITHUB_ENV"

      - name: Verify NSS libraries and environment
        run: |
            echo "=== Library Path Diagnostics ==="
            echo "LD_LIBRARY_PATH=${LD_LIBRARY_PATH}"
            echo "PKG_CONFIG_PATH=${PKG_CONFIG_PATH}"
            echo ""
            echo "=== NSS Libraries in /usr/lib ==="
            ls -la /usr/lib/libnss* /usr/lib/libnspr* 2>/dev/null || \
              echo "Not found in /usr/lib"
            echo ""
            echo "=== NSS Libraries in /usr/lib/aarch64-linux-gnu ==="
            ls -la /usr/lib/aarch64-linux-gnu/libnss* \
              /usr/lib/aarch64-linux-gnu/libnspr* 2>/dev/null || \
              echo "Not found in /usr/lib/aarch64-linux-gnu"
            echo ""
            echo "=== ldconfig cache ==="
            ldconfig -p | grep -E '(nss|nspr)' || \
              echo "NSS/NSPR not in ldconfig cache"
            echo ""
            echo "=== pkg-config ==="
            pkg-config --exists nss && \
              echo "NSS found: $(pkg-config --modversion nss)" || \
              echo "NSS not found by pkg-config"
            pkg-config --exists nspr && \
              echo "NSPR found: $(pkg-config --modversion nspr)" || \
              echo "NSPR not found by pkg-config"

      - name: Verify Python can import nss module
        run: |
            echo "=== Python Import Diagnostics ==="
            python3 -c "import sys; print('Python sys.path:', sys.path)" \
              || true
            echo ""
            echo "=== Attempting to import nss ==="
            python3 -c "import nss; print('SUCCESS: nss imported')" 2>&1 \
              || echo "FAILED: Cannot import nss"
            echo ""
            echo "=== Checking for ImportError details ==="
            python3 -c "
            import sys
            try:
                import nss.nss
                print('SUCCESS: nss.nss imported')
            except ImportError as e:
                print(f'ImportError: {e}')
                import traceback
                traceback.print_exc()
            " 2>&1 || true
            echo ""
            echo "=== Checking installed nss package ==="
            python3 -c "import sys; import site; \
              print('site-packages:', site.getsitepackages())" || true
            PYVER="${{ matrix.python-version }}"
            ls -la /home/runner/.local/lib/python${PYVER}/site-packages/nss/ \
              2>/dev/null || echo "nss package not found"

      - name: Test import with actual Python that will run tests
        run: |
            echo "=== Testing import with hostedtoolcache Python ==="
            PYTHON_PATH="/opt/hostedtoolcache/Python/3.12.12/arm64/bin/python3"
            if [ -f "$PYTHON_PATH" ]; then
                echo "Using: $PYTHON_PATH"
                $PYTHON_PATH -c "import sys; print('Python:', sys.executable)"
                echo "Attempting import..."
                $PYTHON_PATH -c "import nss; print('SUCCESS: nss imported')" \
                  || \
                  echo "FAILED to import nss"
                echo ""
                echo "Checking sys.path:"
                $PYTHON_PATH -c "import sys; print('\n'.join(sys.path))"
                echo ""
                echo "Trying to import nss.error:"
                $PYTHON_PATH -c "import nss.error; print('SUCCESS')" \
                  2>&1 || true
            else
                echo "Python not found at $PYTHON_PATH, trying python3.12"
                which python3.12 && \
                  python3.12 -c "import nss; print('SUCCESS')" || \
                  echo "FAILED with python3.12"
            fi

      - name: "Python tests [pytest] (arm64) ${{ matrix.python-version }}"
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-test-action@92d4110d44ebc18fa4575c6b00203ff67d01a1cb  # v1.0.1
        with:
          python_version: ${{ matrix.python-version }}
          # yamllint disable-line rule:line-length
          artefact_name: python-nss-ng-arm64-py${{ matrix.python-version }}-coverage
          report_artefact: 'true'
        env:
          LD_LIBRARY_PATH: /usr/lib:/usr/lib/aarch64-linux-gnu
          # yamllint disable-line rule:line-length
          PYTHONPATH: /opt/hostedtoolcache/Python/3.12.12/arm64/lib/python3.12/site-packages

      - name: Post-test diagnostics (if test failed)
        if: failure()
        run: |
            echo "=== POST-TEST DIAGNOSTICS ==="
            echo "Checking where package was actually installed..."
            python3 -m pip list | grep python-nss-ng || \
              echo "Package not in pip list"
            echo ""
            echo "Getting pip show output:"
            python3 -m pip show python-nss-ng || echo "pip show failed"
            echo ""
            echo "Extracting actual installation location:"
            INSTALL_LOC=$(python3 -m pip show python-nss-ng | \
              grep "Location:" | cut -d' ' -f2)
            echo "Package installed at: ${INSTALL_LOC}"
            if [ -n "${INSTALL_LOC}" ]; then
                echo "Checking for nss package in actual location:"
                ls -la "${INSTALL_LOC}/nss/" 2>/dev/null || \
                  echo "nss directory not found at ${INSTALL_LOC}"
                echo ""
                echo "Checking for .so files in actual location:"
                find "${INSTALL_LOC}/nss/" -name "*.so" 2>/dev/null || \
                  echo "No .so files found"
                echo ""
                echo "Checking library dependencies with ldd:"
                for so_file in "${INSTALL_LOC}/nss/"*.so; do
                    if [ -f "$so_file" ]; then
                        echo "--- $(basename "$so_file") ---"
                        ldd "$so_file" 2>&1 | \
                          grep -E "(nss|nspr|not found)" || true
                    fi
                done
            fi
            echo ""
            echo "Searching for nss package in all locations:"
            PYVER="${{ matrix.python-version }}"
            find /home/runner/.local/lib/python${PYVER}/ -name "nss" \
              -type d 2>/dev/null || echo "Not found in .local"
            find /usr/local/lib/python${PYVER}/ -name "nss" \
              -type d 2>/dev/null || echo "Not found in /usr/local"
            find /usr/lib/python3/dist-packages/ -name "nss" \
              -type d 2>/dev/null || echo "Not found in dist-packages"
            echo ""
            echo "Searching for ANY python-nss-ng files:"
            find /home/runner/.local/lib/python${PYVER}/ \
              -name "*python*nss*" 2>/dev/null | head -20 || true
            find /usr/local/lib/python${PYVER}/ \
              -name "*python*nss*" 2>/dev/null | head -20 || true
            echo ""
            echo "Searching for .so files with nss:"
            find /home/runner/.local/lib/python${PYVER}/ \
              -name "*nss*.so" 2>/dev/null || echo "No .so files found"
            echo ""
            echo "Checking pip cache:"
            ls -la /home/runner/.cache/pip/wheels/ 2>/dev/null || true

  python-audit-x64:
    name: "Python Audit x64 ${{ matrix.python-version }}"
    runs-on: 'ubuntu-latest'
    needs: ['python-metadata', 'python-build-x64']
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.python-metadata.outputs.matrix_json) }}"
    permissions:
      contents: read
    timeout-minutes: 10
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: Build NSS/NSPR and install dependencies
        run: |
          make deps-nss deps-test env-github-actions

      - name: "Audit dependencies ${{ matrix.python-version }}"
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-audit-action@ec8d84ca14c0413a2b2c6612a3e15b9803f9de75  # v0.2.5
        with:
          python_version: ${{ matrix.python-version }}
          # yamllint disable-line rule:line-length
          artefact_name: python-nss-ng-x64-py${{ matrix.python-version }}

  python-audit-arm64:
    name: "Python Audit ARM64 ${{ matrix.python-version }}"
    runs-on: 'ubuntu-24.04-arm'
    needs: ['python-metadata', 'python-build-arm64']
    strategy:
      fail-fast: false
      matrix: "${{ fromJson(needs.python-metadata.outputs.matrix_json) }}"
    permissions:
      contents: read
    timeout-minutes: 10
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: Build NSS/NSPR and install dependencies
        run: |
          make deps-nss deps-test env-github-actions

      - name: "Audit dependencies ${{ matrix.python-version }}"
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-audit-action@ec8d84ca14c0413a2b2c6612a3e15b9803f9de75  # v0.2.5
        with:
          python_version: ${{ matrix.python-version }}
          # yamllint disable-line rule:line-length
          artefact_name: python-nss-ng-arm64-py${{ matrix.python-version }}

  sbom-x64:
    name: "Generate SBOM x64"
    runs-on: ubuntu-latest
    needs: ['python-metadata', 'python-build-x64']
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: Build NSS/NSPR from source
        run: |
          make deps-nss env-github-actions

      - name: "Generate SBOM"
        id: sbom
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-sbom-action@ae4aca2ef28d7da4ec95049cc78be43e632d322a  # v0.1.0
        with:
          include_dev: "false"
          sbom_format: "both"

      - name: "Upload SBOM artifacts"
        # yamllint disable-line rule:line-length
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        with:
          name: sbom-files-x64
          path: |
            sbom-cyclonedx.json
            sbom-cyclonedx.xml
          retention-days: 45

      - name: "Security scan with Grype (SARIF)"
        # yamllint disable-line rule:line-length
        uses: anchore/scan-action@0d444ed77d83ee2ba7f5ced0d90d640a1281d762  # v7.3.0
        id: grype-sarif
        with:
          sbom: "${{ steps.sbom.outputs.sbom_json_path }}"
          output-format: "sarif"
          output-file: "grype-results.sarif"
          fail-build: "true"

      - name: "Security scan with Grype (Text/Table)"
        # yamllint disable-line rule:line-length
        uses: anchore/scan-action@0d444ed77d83ee2ba7f5ced0d90d640a1281d762  # v7.3.0
        id: grype-table
        if: always()
        with:
          sbom: "${{ steps.sbom.outputs.sbom_json_path }}"
          output-format: "table"
          output-file: "grype-results.txt"
          fail-build: "false"

      - name: "Upload Grype scan results"
        # yamllint disable-line rule:line-length
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        if: always()
        with:
          name: grype-scan-results-x64
          path: |
            grype-results.sarif
            grype-results.txt
          retention-days: 90

      - name: "Grype summary"
        if: always()
        run: |
            # Grype summary
            echo "## Grype Summary (x64)" >> "$GITHUB_STEP_SUMMARY"
            [ -f grype-results.txt ] && cat grype-results.txt \
              >> "$GITHUB_STEP_SUMMARY" || echo "No scan results available" \
              >> "$GITHUB_STEP_SUMMARY"

  sbom-arm64:
    name: "Generate SBOM ARM64"
    runs-on: ubuntu-24.04-arm
    needs: ['python-metadata', 'python-build-arm64']
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      # Harden the runner used by this workflow
      # yamllint disable-line rule:line-length
      - uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76  # v2.14.0
        with:
          egress-policy: 'audit'

      # yamllint disable-line rule:line-length
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8  # v6.0.1

      - name: Build NSS/NSPR from source
        run: |
          make deps-nss env-github-actions

      - name: "Generate SBOM"
        id: sbom
        # yamllint disable-line rule:line-length
        uses: lfreleng-actions/python-sbom-action@ae4aca2ef28d7da4ec95049cc78be43e632d322a  # v0.1.0
        with:
          include_dev: "false"
          sbom_format: "both"

      - name: "Upload SBOM artifacts"
        # yamllint disable-line rule:line-length
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        with:
          name: sbom-files-arm64
          path: |
            sbom-cyclonedx.json
            sbom-cyclonedx.xml
          retention-days: 45

      - name: "Security scan with Grype (SARIF)"
        # yamllint disable-line rule:line-length
        uses: anchore/scan-action@0d444ed77d83ee2ba7f5ced0d90d640a1281d762  # v7.3.0
        id: grype-sarif
        with:
          sbom: "${{ steps.sbom.outputs.sbom_json_path }}"
          output-format: "sarif"
          output-file: "grype-results.sarif"
          fail-build: "true"

      - name: "Security scan with Grype (Text/Table)"
        # yamllint disable-line rule:line-length
        uses: anchore/scan-action@0d444ed77d83ee2ba7f5ced0d90d640a1281d762  # v7.3.0
        id: grype-table
        if: always()
        with:
          sbom: "${{ steps.sbom.outputs.sbom_json_path }}"
          output-format: "table"
          output-file: "grype-results.txt"
          fail-build: "false"

      - name: "Upload Grype scan results"
        # yamllint disable-line rule:line-length
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6.0.0
        if: always()
        with:
          name: grype-scan-results-arm64
          path: |
            grype-results.sarif
            grype-results.txt
          retention-days: 90

      - name: "Grype summary"
        if: always()
        run: |
            # Grype summary
            echo "## Grype Summary (ARM64)" >> "$GITHUB_STEP_SUMMARY"
            [ -f grype-results.txt ] && cat grype-results.txt \
              >> "$GITHUB_STEP_SUMMARY" || echo "No scan results available" \
              >> "$GITHUB_STEP_SUMMARY"
